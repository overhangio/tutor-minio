minio:
  ports:
    - "127.0.0.1:9000:9000"
    - "127.0.0.1:9001:9001"
  environment:
    MINIO_ROOT_USER: "{{ OPENEDX_AWS_ACCESS_KEY }}"
    MINIO_ROOT_PASSWORD: "{{ OPENEDX_AWS_SECRET_ACCESS_KEY }}"
  networks:
    default:
      aliases:
        - "{{ MINIO_HOST }}"
